# qiven-runtime

Bootstrap repository for the `qiven::runtime` C++ library.

This README is bootstrap-only and belongs to this repository after generation. Replace it with domain documentation as the
library is designed. Shared engineering conventions are under `docs/engineering/`.
