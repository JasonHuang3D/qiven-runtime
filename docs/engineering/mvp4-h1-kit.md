# MVP-4 H1 Acceptance — Kit Pointer (not the kit)

The MVP-4 row-1 H1 acceptance (real-ZCode deny-no-fallthrough) is a
PACKAGE, not this document. The 2026-09-23 owner direction after the
deny-118 incident: an H1 kit the owner must interpret by reading prose
here is a defect class — H1 means the owner's HANDS run a prepared,
complete, self-rollback-able package, like a deployment.

Build and run:

```cmd
cd /d D:\JasonWork\qiven-runtime
tools\qiven.cmd run build-release
tools\qiven.cmd gate local
python tools\h1_kit.py
```

The kit lands under `<workspace>\h1-kits\qiven-runtime\mvp4-h1\<version>-g<sha>\`
with `GUIDE.md` as the owner entry point: ready `config.json` (no owner
authoring; the ZCode UI review is the enable gate), double-clickable
`run-host.cmd` / `stop-host.cmd` / `collect-evidence.cmd` /
`rollback.cmd`, Release binaries, payload-probe capture, and a digest
manifest bound to the exact validated head.

The probe definitions (P1-P6) and grading rubric live in the kit's
GUIDE.md, generated by `tools/h1_kit.py` — single source, no drift
between a doc here and the package there.
